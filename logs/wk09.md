# Review
- IDs need hashtags # to be represented as IDs

# What I learned
- where is the <script> tag     - above and below javascript text
- How does JavaScript find HTML elements on the page?       - DOM, browsers live tree of HTML, allows for document searching
- What makes the "add" button actually do something?        - addEventListener("click", ...).the button itseld is html, but when clicked this callback function runs
- Where does the code change the page (add text, add items, etc.)?      - setting text, creating elements, inserting/removing files, changing attibutes/classes/styles
- variables/statements/data types to make it more readable
- addeventlistener = core of javascript code
- organizing files between html, css, and javascript

Declaring variables (let and const)
- use let for values that change
- use const for values that stay the same
- avoid using var

Naming
- start with letter, _, or $
- camelCase for variables: userName, totalScore
- UPPER_CASE for constants: MAX_SIZE, API_KEY

Data types
- primitive: string, number, boolean, undefined, null
- basic operators

# Questions

# Extra files
- greeting.html
- s16-demo.html
- s17-demo.html

# To do
- MP2
